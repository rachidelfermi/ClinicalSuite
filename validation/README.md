# Validation assets

This directory contains non-patient fixtures, expected outputs, and validation
helpers. GIAB and other large external validation data remain outside git and are
referenced through approved manifests; they are never downloaded by the pipeline.
